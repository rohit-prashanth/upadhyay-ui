export class commonConstants {
    public static storageKeys = {
        authToken: 'authToken',
        refreshToken: 'refreshToken'
    }

    public static APIURL = {
        loginUser: "{DOMAIN01}/login/",
        viewUser: "{DOMAIN01}/view-user/",
        logoutUser: "{DOMAIN01}/logout/"
    }
}